'use strict'

module.exports = {
  up: async () => {
    // No hace falta nada si ya no quieres correrla
  },

  down: async () => {
    // Al menos esta función debe existir para que el undo no falle
  }
}
